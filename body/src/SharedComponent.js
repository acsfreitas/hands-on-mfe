import React from "react";
import "./style.css";

export const SharedComponent = (props) => {
  return (
    <div className="shared_component">Componente compartilhado do body</div>
  );
};

export default SharedComponent;
